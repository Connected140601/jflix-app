// JFlix iOS/iPadOS PWA Install Modal - DISABLED
// The "Add to Home Screen" modal has been removed completely.
// iOS users can browse the website directly in Safari or any browser
// without being prompted to install the PWA.
(function() {
  'use strict';
  // Do nothing — modal disabled per request
  return;
})();
